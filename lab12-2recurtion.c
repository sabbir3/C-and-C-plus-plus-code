
#include<stdio.h>
int big-mode(int base,int power, int mod)
{
    if(power==0) return 1;
    else if (power/2==1);

{
    int p1=base%mod;
    int (p2=(big-mod(base,power-1,mod))%mod;
    return(p1*P2)%mod;

}
