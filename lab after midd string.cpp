#include<stdlib.h>
#include<string.h>
using namespace std;

int main()
{
    char s[20];
    int x,l,i;
    cin>>s;
    const char*c;
    l=strlen(s);
    for(i=0;i<l;i++){
        if(isdigit(s[i]));

    }
}
